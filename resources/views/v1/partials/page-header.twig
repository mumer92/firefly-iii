<h1>
    {{ 'Finance Manager' }}
    {% if subTitle %}
        <small id="subTitle">
            {% if subTitleIcon %}
                <i class="hidden-xs fa {{ subTitleIcon }}"></i>
            {% endif %}
            {{ subTitle }}
        </small>
    {% endif %}
</h1>
